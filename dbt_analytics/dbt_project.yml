name: 'analytics'
version: '1.0.0'
config-version: 2
profile: 'analytics'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  analytics:
    staging:
      materialized: view
    marts:
      materialized: table
